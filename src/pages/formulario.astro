---
import MainLayout from "~/layouts/MainLayout.astro";
import PracticanteForm from "~/components/PracticanteForm.jsx";
import { Plus, ArrowLeft } from "lucide-react";
---
<MainLayout title="Registro de Practicante | FESC">
  <div class="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100 py-8">
    <div class="max-w-4xl mx-auto px-4">
      <!-- Header público -->
      <div class="text-center mb-8">
        <div class="flex justify-center mb-6">
          <div class="w-16 h-16 bg-accent rounded-full flex items-center justify-center shadow-lg">
            <Plus className="w-8 h-8 text-white" />
          </div>
        </div>
        <h1 class="text-4xl font-bold text-gray-800 mb-4">
          Registro de <span class="text-accent">Practicante</span>
        </h1>
        <p class="text-lg text-gray-600 max-w-2xl mx-auto">
          Completa este formulario para registrarte como practicante en una empresa externa. 
          Todos los campos marcados con (*) son obligatorios.
        </p>
      </div>

      <!-- Navegación de regreso -->
      <div class="mb-6">
        <a href="/" class="inline-flex items-center text-gray-600 hover:text-accent transition-colors">
          <ArrowLeft className="w-5 h-5 mr-2" />
          Volver al inicio
        </a>
      </div>
      
      <PracticanteForm client:only="react" />
      
      <!-- Footer informativo -->
      <div class="mt-8 text-center">
        <div class="bg-blue-50 rounded-xl p-6 max-w-2xl mx-auto">
          <h3 class="text-lg font-semibold text-blue-900 mb-2">¿Necesitas ayuda?</h3>
          <p class="text-blue-700 text-sm">
            Si tienes alguna pregunta o necesitas asistencia con el registro, 
            puedes contactar al departamento de prácticas profesionales de la FESC.
          </p>
        </div>
      </div>
    </div>
  </div>
</MainLayout>
